%h1 Sign in
= form_for(:session, url: sessions_path) do |f|
  = f.label :email
  = f.email_field :email

  = f.label :password
  = f.password_field :password

  = f.submit "Sign in"

%p New user?
= link_to "Sign up now!", signup_path
